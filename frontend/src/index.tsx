import React from 'react';
import ReactDOM from 'react-dom';
import { AwesomeComp } from "@lamsal-de/components"

ReactDOM.render(
  <React.StrictMode>
    <AwesomeComp />
  </React.StrictMode>,
  document.getElementById('root')
);
